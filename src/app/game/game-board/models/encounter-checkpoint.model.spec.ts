import { CHECKPOINT_VERSION } from './encounter-checkpoint.model';

describe('encounter-checkpoint.model', () => {
  it('CHECKPOINT_VERSION is 11', () => {
    expect(CHECKPOINT_VERSION).toBe(11);
  });
});
