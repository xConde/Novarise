export const PATH_LINE_CONFIG = {
  /** Gold/yellow color for high visibility against dark board tiles. */
  color: 0xffd700,
  dashSize: 0.3,
  gapSize: 0.2,
  lineWidth: 2,
  /** Slightly above ground so the line is visible over tile surfaces. */
  yOffset: 0.15,
  opacity: 0.75,
} as const;
